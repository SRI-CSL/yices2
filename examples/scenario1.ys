(echo "base\n")
(dump-context)
(push)
(define a::int)
(define b::int)
(define c::int)
(assert (= (+ a b c) 0))
(echo "after (assert (= (+ a b c) 0))\n")
(dump-context)
(check)
(echo "after (check)\n")
(dump-context)
(define d::int)
(assert (> (+ a d) 0))
(dump-context)
(echo "after (assert (> (+ a d) 0))\n")
(check)
(echo "after (check)\n")
(dump-context)
(pop)
(echo "after (pop)\n")
(dump-context)
