name: Coverage

runs:
  using: composite
  steps:
    - name: Run Coverage
      shell: bash
      run: |
        lcov --directory build --base-directory src --capture --ignore-errors source --output-file coverage.info
